export type ButtonVariants = 'default' | 'link' | 'navlink' | 'button';

export const buttonVariants: Record<ButtonVariants, string> = {
  default: '',
  link: '',
  navlink: 'text-sm font-semibold leading-6 text-gray-900',
  button: '',
}
